# management

Management group: **Management** (id `management`), child of `platform`.

Subscription folder: `sub-management/`. Holds the central logging and
monitoring resources (`log-analytics-central`) under
`sub-management/westeurope/`.

Scaffold only. `subscription.hcl`, `region.hcl`, and the unit config are not
written yet.
