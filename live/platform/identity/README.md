# identity

Management group: **Identity** (id `identity`), child of `platform`.

No subscription provisioned in v1 (0 subscriptions in the reference tenant).
When an identity subscription is added, create a `sub-identity/` folder here
following the same layout as the other platform MGs.
