# security

Management group: **Security** (id `security`), child of `platform`.

No subscription provisioned in v1 (0 subscriptions in the reference tenant).
When a security subscription is added, add a `subscription.hcl` (and a
`region.hcl` per region) directly under this folder, the same flattened
layout used by `connectivity` and `management`.
