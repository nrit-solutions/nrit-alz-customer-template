# security

Management group: **Security** (id `security`), child of `platform`.

No subscription provisioned in v1 (0 subscriptions in the reference tenant).
When a security subscription is added, create a `sub-security/` folder here
following the same layout as the other platform MGs.
