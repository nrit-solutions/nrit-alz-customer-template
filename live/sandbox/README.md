# sandbox

Management group: **Sandbox** (id `sandbox`), child of `alz`.

Loosely governed subscriptions for experimentation, outside the corp and
online policy sets. No subscription folders in v1; add a named subscription
folder here when a sandbox subscription is provisioned.
