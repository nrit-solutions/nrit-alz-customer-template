# Canonical caller for the reusable drift workflow.
# Copy to a consumer repo as .github/workflows/drift.yml. Bump the two version
# pins (the uses: ref and engine_ref) together to move to a new engine release.
name: drift

on:
  schedule:
    - cron: "17 6 * * *"
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  id-token: write

jobs:
  engine:
    uses: nrit-solutions/nrit-tf-pr-ops/.github/workflows/drift.yml@v2.1.0
    with:
      engine_ref: v2.1.0   # must match the uses: ref above
    secrets: inherit
